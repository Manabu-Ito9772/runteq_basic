<%= form_with model: current_user.bookmarks.build, local: true do |f| %>

  <%= hidden_field_tag :board_id, board.id %>

  <%= f.button :type => 'submit', id: "js-bookmark-button-for-board-#{board.id}" do %>
    <%= icon 'far', 'star' %>
  <% end %>  

<% end %>
